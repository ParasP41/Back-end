<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none px-5 py-5 bg-zinc-800" type="text"
                    placeholder="title" name="title">
                <textarea class="block w-full rounded-md outline-none px-5 mt-2 py-5 resize-none bg-zinc-800"
                    name="description" placeholder="description" id=""></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit">
            </form>
        </div>
        <div class="tasks flex gap-4 flex-wrap p-10 ">
            <% if(files.length>0) { %>
                <% files.forEach((val)=> { %>
                    <div class="task min-w-72 px-3 rounded-md py-4 bg-zinc-800 ">
                        <h1 class="text-white text-2xl tracking-tighter">
                            <%= val %>
                        </h1>
                        <div class="flex item-center justify-between w-full mt-3">
                            <a class="text-blue-500 inline-block" href="/files/<%= val %>">Read more</a>
                            <a class="text-zinc-400 inline-block" href="/edit/<%= val %>">edit</a>
                        </div>
                    </div>
                    <%})%>
                        <% } else { %>
                            <h3 class="text-zinc-400 ">No task yet..</h3>
                            <% } %>
        </div>
    </div>
</body>

</html>