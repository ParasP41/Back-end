<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a class="bg-red-600 rounded-md px-3 py-2 inline-block mb-5" href="/logout">Log Out</a>
        </div>
        <h3 class="text-3xl"><span class="font-light">Hello</span>, <%=user.name%> 👋</h3>
        <h5 class="mb-5 ">You Can Create the new Post</h5>
        <form action="/post" method="post">
            <textarea placeholder="What's in Your Mind"
                class="resize-none p-6 bg-transparent outline-none border-2 block border-zinc-800 w-1/3 rounded-md"
                name="content"></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-4 " type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-2xl text-zinc-400">Your Post</h3>
            <div class="postContainer mt-6">
                <%if(user.post.length>0){%>
                    <%user.post.reverse().forEach((post)=>{%>
                        <div class="post w-1/3 p-5 mb-3 rounded-md bg-zinc-800 border-2 border-zinc-800 ">
                            <h4 class="text-blue-500 mb-4">@<%=user.username%></h4>
                            <p class="tracking-tight"><%=post.content%></p>
                            <div class="btn flex mt-5 gap-10">
                                <a class="text-blue-500" href="">Like</a>
                                <a class="text-zinc-400" href="">edit</a>
                            </div>
                        </div>
                        <%})%>
                            <%}else{}%>


            </div>
        </div>
    </div>
</body>

</html>