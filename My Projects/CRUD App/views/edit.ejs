<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit To-Do With Previous Data</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">

  <form action="/update/<%=todo._id%>" method="post" class="bg-white p-8 rounded-2xl shadow-xl w-full max-w-md space-y-6">
    <h2 class="text-2xl font-bold text-center text-gray-700">Edit To-Do</h2>

    <!-- Previous Image -->
    <div>
      <label class="block text-sm font-medium text-gray-600 mb-1">Previous Image</label>
      <img src="<%=todo.imageurl%>" alt="Previous Image"
           class="w-full h-40 object-cover rounded-lg border mb-3" />
      <input
        type="url"
        name="imageUrl"
        placeholder="Enter new image URL"
        class="w-full px-4 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
    </div>

    <!-- Previous Title -->
    <div>
      <label class="block text-sm font-medium text-gray-600 mb-1">Previous Title</label>
      <div class="text-gray-800 font-semibold mb-2"><%=todo.title%></div>
      <input
        type="text"
        name="title"
        placeholder="Enter new title"
        class="w-full px-4 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
    </div>

    <!-- Previous Description -->
    <div>
      <label class="block text-sm font-medium text-gray-600 mb-1">Previous Description</label>
      <div class="text-gray-700 mb-2"><%=todo.description%></div>
      <textarea
        name="description"
        rows="4"
        placeholder="Enter new description"
        class="w-full px-4 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
      ></textarea>
    </div>
    <div >
      <button type="submit"
              class="bg-blue-600 w-full text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition">
        Update To-Do
      </button>
    </div>
  </form>

</body>
</html>
